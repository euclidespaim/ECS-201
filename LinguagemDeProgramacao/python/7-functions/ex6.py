# Crie uma função chamada contagem que:
# Receba um número inicial e um número final
# Conte de inicial até final usando while

def contagem(a, b):
  while a <= b:
    print(a)
    a += 1
    
contagem(10, 15)